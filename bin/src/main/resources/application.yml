server:
   port: 8080
   servlet:
    contextPath: /burguer

spring:
  application:
    name: burguersystem
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/burguersystem
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 123456789
    platform: mariadb
    driverClassName: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
    generate-ddl: false
    hibernate:
      ddl-auto: none
